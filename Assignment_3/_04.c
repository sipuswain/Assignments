#include<stdio.h>
//to check a given no.even or odd (without using %)
int main()
{
   int a;
   scanf("%d",&a);
   if(a>0)
   {
   printf("positive");
   }
   else
   printf("non positive");
   return 0;
}
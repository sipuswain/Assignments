#include<stdio.h>
//to check a given no. divisible by 5 or not
int main()
{
   int a;
   scanf("%d",&a);
   if(a%5)
   {
   printf("not divisible");
   }
   else
   printf("divisible");
   return 0;
}